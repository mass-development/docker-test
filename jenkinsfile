pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/mass-development/docker-test'
                sh 'echo "checking out the repository"'
            }
        }
    }
}
