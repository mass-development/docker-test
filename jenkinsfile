pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                sh 'echo "checking out the repository"'
            }
        }
        stage('build') {
            steps {
                sh 'echo "building the repository"'
            }
        }
    }
}
