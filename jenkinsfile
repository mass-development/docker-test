pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/mass-development/docker-test'
                sh 'echo "checking out the repository"'
            }
        }
    }
}
