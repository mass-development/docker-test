pipeline {
    agent any
    stages {
        stage('checkout') {
            step {
                git 'https://github.com/mass-development/docker-test'
                sh 'checking out the repository'
            }
        }
    }
}
